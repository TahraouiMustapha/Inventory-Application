<html>
    <head>
        <title><%= title %></title>
    </head>
    <body>
        <ul>
            <% movies.forEach(movie => {%>
            <li><%= movie.title %>, <%=movie.movieid%> <a href="<%= `/movies/${movie.movieid}` %>">click here</a></li>  
            <%})%>      
        </ul>
        
    </body>
</html>
